# Node TypeScript Basic Fixture

Small fixture repo for runner integration tests. It is intentionally runnable
without installing dependencies so sandbox tests can execute it with network
access disabled.
